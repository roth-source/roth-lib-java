package roth.lib.java.net.http;


public class HttpClientTest
{
	
	public static void main(String[] args) throws Exception
	{
		/*
		HttpClient client = new HttpClient();
		HttpUrl url = new HttpUrl("http://avpayment.com/launch/json");
		HttpConnection connection = client.connect(url);
		RequestHeaders headers = new RequestHeaders();
		//headers.addHeader("User-Agent", "Mozilla/5.0 (Macintosh; Intel Mac OS X 10.9; rv:35.0) Gecko/20100101 Firefox/35.0");
		//headers.addHeader("Connection", "keep-alive");
		//headers.addHeader("Accept-Language", "en-US,en;q=0.5");
		headers.setHost(new Host(url.getHost()));
		Request request = new Request().setUrl(url).setHeaders(headers);
		Response<String> response = connection.connect(request, new StringStreamDeserializer());
		System.out.print(request);
		System.out.print(response);
		System.out.println(response.getContent());
		*/
	}
	
}
